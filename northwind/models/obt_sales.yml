version: 2

models:
  - name: obt_sales
    description: The sales fact table combined with all of its relevant dimensions into one big table. Transaction fact table with one row per line item ordered, including product data.